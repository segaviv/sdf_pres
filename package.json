{
  "name": "sdf_presentation",
  "version": "1.0.0",
  "description": "SDF presentation.",
  "scripts": {
    "start": "parcel *.html live_stuff/latest/*.html  figs/*.html slides/*.html slides/*/*.html",
    "predeploy": "rm -rf dist && parcel build --public-url 'https://segaviv.github.io/sdf_pres' *.html  figs/*.html slides/*.html slides/*/*.html slides/res/*",
    "deploy": "gh-pages -d dist"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/reveal": "^4.2.0",
    "gh-pages": "^4.0.0",
    "parcel": "^2.6.2",
    "reveal.js": "^4.3.1"
  }
}
